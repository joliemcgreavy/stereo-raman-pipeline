# Deployment requirements for Streamlit dashboard only.
# Excludes open3d (no Linux wheel), ramanspy (unused in dashboard),
# jupyter/pytest (dev tools not needed at runtime).

numpy>=1.26
scipy>=1.13
opencv-python-headless>=4.9
scikit-learn>=1.5
pandas>=2.2
matplotlib>=3.9
seaborn>=0.13
plotly>=5.22
streamlit>=1.35
